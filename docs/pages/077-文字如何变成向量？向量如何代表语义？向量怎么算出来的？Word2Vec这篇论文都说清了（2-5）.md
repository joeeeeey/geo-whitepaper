# 文字如何变成向量？向量如何代表语义？向量怎么算出来的？Word2Vec这篇论文都说清了（2/5）

这一页是整篇论文的核心！作者基于第三页的分析，提出了两个全新的、旨在“最小化计算复杂度”的模型。

3. 新的对数线性模型 (New Log-linear Models)

• 核心决策 :

“most of the complexity is caused by the non-linear hidden layer in the model. (…) we decided to explore simpler models that (…) can possibly be trained on much more data efficiently.”

• 翻译与解读 : 既然大部分的计算开销都来自非线性隐藏层，那我们就干脆探索一些更简单的模型，这些模型可能在表达能力上不如神经网络那么强，但优点是训练速度极快，能用在更多的数据上。

• 这是一种工程和研究上的权衡（trade-off） ：用模型的简化，换取在更大规模数据上训练的能力。他们赌的是，“海量数据 + 简单模型” > “少量数据 + 复杂模型”。事后证明，他们赌赢了。

---

3.1 连续词袋模型 (Continuous Bag-of-Words Model, CBOW)

这是他们提出的第一个模型。

• 架构思想 :

• 为什么叫“词袋”（Bag-of-Words）? 因为在取平均时，我们不考虑上下文中词的顺序。就像把所有词扔进一个袋子里，只关心有哪些词，不关心它们的排列。例如，“我 爱 你”和“你 爱 我”的上下文词袋是一样的。

1. 移除隐藏层 : 这是最关键的一步，直接砍掉了 N × D × H 这个计算瓶颈。

2. 共享投影 : 所有上下文单词共享一个投影矩阵（这和NNLM一样）。

3. 取平均 : 将上下文所有单词的向量进行 平均 ，形成一个综合的上下文向量。

• 任务 : 根据上下文预测中间的词。

• 类比：“完形填空” 。

• 给你一句话 [the, quick, brown, ___, jumps, over] 。CBOW模型会把上下文 [the, quick, brown, jumps, over] 这几个词的向量加起来取个平均，然后用这个平均向量去预测中间的空白处最应该是 fox 。

• 计算复杂度 : Q = N × D + D × log₂(V)

• N : 上下文窗口大小（比如前后各4个词，N=8）。

• D : 词向量维度。

• log₂(V) : 使用了Hierarchical Softmax优化后的输出层复杂度。

• 对比一下NNLM的 N × D + N × D × H + H × log₂(V) ，你会发现CBOW把最昂贵的 N × D × H 去掉了！ 这使得它的速度快了几个数量级。

---

3.2 Skip-gram 模型 (Continuous Skip-gram Model)

这是他们提出的第二个模型，也是后来被证明在很多任务上效果更好的模型。

• 架构思想 : 这个模型做的事情和CBOW正好 相反 。

• 任务 : 根据中间的词预测上下文。

• 类比：“一词生万物” 或 “思维发散” 。

• 给你一个中心词 fox 。Skip-gram模型的任务是预测它周围可能出现哪些词，比如 the , quick , brown , jumps , over 。

• 训练细节 :

• 对于每个输入的词，模型会从其上下文（比如前后5个词）中随机选择几个词作为“正例”（需要被预测出来的词）。

• 作者还发现，在训练时，对离中心词越远的词，可以稍微降低它们的权重（通过减少采样率），因为它们的关联性通常没那么强。

• 计算复杂度 : Q = C × (D + D × log₂(V))

• C : 上下文窗口的最大距离 (range)。

• 解读 : 对于每个输入的词，我们需要做 C 次“预测上下文”的分类任务。虽然看起来比CBOW的 Q 要大，但它在学习单词的语义表示上往往更有效。

---

第四页总结

• 核心创新 : 砍掉昂贵的非线性隐藏层 ，设计了两个更简单、更快的“对数线性”模型。

• CBOW (连续词袋模型) :

• 任务 : 完形填空（用周围词预测中心词）。

• 特点 : 速度飞快，对上下文信息进行了平滑处理（取平均）。

• Skip-gram (跳字模型) :

• 任务 : 思维发散（用中心词预测周围词）。

• 特点 : 能更好地学习到罕见词的表示，对语义关系的捕捉通常更精准，但计算量稍大。

学完第四页，你已经掌握了Word2Vec的两个核心模型CBOW和Skip-gram的原理。

但这里需要加个餐，咱们展开说说 隐藏层是什么 和 向量的值是怎么算出来的。

什么是隐藏层？

在神经网络的结构中，你可以把它想象成一个三明治：

• 输入层 (Input Layer) ：最下面那片面包，负责接收原始数据。比如一张图片的像素点，一句话里的单词。这是我们能直接看到和控制的部分。

• 输出层 (Output Layer) ：最上面那片面包，负责给出最终结果。比如判断图片“是猫还是狗”，或者预测下一个单词“是什么”。这也是我们能直接看到和关心的结果。

• 隐藏层 (Hidden Layers) ：夹在中间的所有“馅料”。它可以是一层，也可以是很多层（这时候就叫“深度”神经网络了）。

隐藏层是神经网络的 “大脑”和“工作引擎” ，它最重要的作用有两个： 特征提取与抽象 和 引入非线性 。这里就不继续展开了。总之，隐藏层就是更 “聪明” 但很 “费体力” 的工作单位。

为什么叫“隐藏”层？

因为它对于使用者来说是不透明的。我们定义了它的存在（比如设置几层，每层多少个神经元），但我们不直接控制它内部具体的计算数值，也看不到它每一刻的中间状态。它的工作是在“幕后”默默进行的，由模型在训练过程中自己学习和调整。

从结构上看，隐藏层是由许多“神经元”（Neurons）组成的。 每个神经元都是一个微小的计算单元，它会：

1. 接收来自上一层所有神经元的信号。

2. 对这些信号进行一次“加权求和”（代表了不同输入信号的重要性）。

3. 将这个和通过一个叫做 “激活函数” 的东西进行处理。

4. 将处理后的结果传递给下一层。

示例：识别一张猫的图片

• 输入层 ：64x64像素的图片（4096个像素值）。

• 第一个隐藏层 ：可能学会识别一些非常基础的 边缘、曲线和色块 。这个神经元可能因为看到一个“向上的弧线”而被激活，那个神经元可能因为看到一块“毛茸茸的黄色纹理”而被激活。

• 第二个隐藏层 ：将第一层的简单特征组合起来。它可能会学到，把一个“向上的弧线”和另一个“向上的弧线”组合起来，就是“猫耳朵”的特征；把两个“圆形的色块”组合起来，就是“眼睛”的特征。

• 更深的隐藏层 ：将“猫耳朵特征”、“眼睛特征”、“胡须特征”、“猫鼻子特征”组合起来，最终形成一个非常抽象的“猫脸”概念。

• 输出层 ：当“猫脸”这个概念被强烈激活时，输出层就会自信地给出“这是一只猫”的判断。

三、为什么Word2Vec要去掉隐藏层？

既然隐藏层这么强大，是神经网络的大脑，那为什么Word2Vec的作者反而要把它去掉呢？

1. 目标不同 ：

• 传统的NNLM（神经网络语言模型）的目标是 尽可能准确地预测下一个词 。为了做到这一点，它需要一个强大的“大脑”（隐藏层）来理解复杂的语法和语义上下文。

• Word2Vec的目标不是预测，而是 高效地学习到词的向量表示 。作者发现，要学到 国王 - 男人 + 女人 ≈ 女王 这种 线性关系 ，一个超级复杂、充满非线性能力的“大脑”反而是“杀鸡用牛刀”，甚至可能有害。

2. 成本与收益的权衡 (Trade-off) ：

• 正如我们之前在论文中看到的，隐藏层是计算成本的 主要来源 ( N×D×H 和 H×H 这两项）。

• 作者做了一个天才般的权衡：我愿意 牺牲模型本身的复杂度（去掉隐藏层），来换取在海量数据上进行极速训练的能力 。

• 他们赌的是： 一个简单的模型，在“阅读”了上亿甚至上百亿的句子之后，学到的词向量质量，会超过一个复杂模型在少量数据上“精读”学到的结果。

事实证明，他们赌对了。对于学习词向量这个特定任务， 数据的广度远比模型的深度更重要 。去掉隐藏层，使得Word2Vec可以史无前例地处理海量文本，从而捕捉到那些微妙而丰富的词义关系。

CBOW和Skip-gram模型是怎么把向量算出来的？

我来用一个类比解释一下： “为了画出美食地图而品尝美食” 。

---

类比：画一张“城市美食地图”

假设你的 最终目标 是画一张“城市美食地图”。这张地图不是普通的地理地图，而是一张 味觉空间地图 。在这张地图上：

• 味道相似的餐厅（比如“四川火锅店”和“重庆串串香”）在地图上的 位置会非常接近 。

• 味道迥异的餐厅（比如“四川火锅店”和“日式甜品店”）在地图上的 位置会离得很远 。

• 每个餐厅在地图上的坐标 (x, y) 就是它的 “味觉向量” 。

你如何得到这张地图呢？ 你不能直接测量“味道”，你只能通过一个 行动（手段） 来学习。

这个 手段 就是： 去城市里成千上万的餐厅吃饭，并且做一个“猜菜”游戏。

这个“猜菜”游戏就是论文里“用词推理词”的过程：

1. 游戏规则 (Skip-gram/词语联想) : 你走进一家餐厅，只点一道招牌菜（比如“毛血旺”）。然后你要 预测 这家餐厅的菜单上 还可能有什么菜 （比如“水煮牛肉”、“麻婆豆腐”、“冰粉”）。

2. 学习过程 :

• 一开始，你对所有餐厅一无所知。你的“美食地图”是一张白纸，上面随机散落着所有餐厅的名字（ 初始的词向量是随机的 ）。

• 你来到“A火锅店”，点了“毛血旺”。你根据你那张乱七八糟的初始地图猜，这家店可能还有“寿司”。结果你错了，菜单上是“水煮牛肉”。

• 关键一步：你如何学习？ 你会拿出你的地图，把“A火锅店”的位置 稍微 往“水煮牛肉”的方向移动一点点，同时 稍微 离“寿司”远一点点。（ 这就是在调整词向量！ ）

• 接着，你又来到“B串串香店”，也点了和“毛血旺”味道很像的菜。你也玩这个游戏，结果发现它的菜单上也有“水煮牛肉”。于是，你又在地图上把“B串串香店”也往“水煮牛肉”的方向移动了一点点。

3. 最终结果 (副产品) :

• 当你把全城的餐厅都吃了个遍，玩了成千上万次“猜菜”游戏之后，会发生什么？

• 因为“A火锅店”和“B串串香店”经常和相似的菜品（水煮牛肉、麻婆豆腐）一起出现，所以它们在地图上的位置，被你一次又一次地 往相似的方向调整 。最终，它们俩在地图上的坐标就会变得非常接近！

• 而“C甜品店”因为总是和“蛋糕”、“布丁”一起出现，所以它的位置会和火锅店离得非常远。

现在，游戏结束了。你可能已经不关心每一道菜你猜对没有了。但你低头一看，你手上那张最初乱七八糟的地图，现在已经变得井井有条。 你得到了你的终极目标——一张高质量的“味觉空间地图”，上面每个餐厅都有了它精确的“味觉向量”！

---

回到 Word2Vec

现在我把类比换回论文：

• 美食地图 -> 词向量空间

• 餐厅 -> 词语

• 餐厅的坐标 -> 词的向量

• 品尝美食、玩猜菜游戏 -> 用一个词预测它周围的词 (Skip-gram)

Word2Vec 的工作流程就是：

1. 目标 : 为词典里每个词找到一个好的向量表示。

2. 手段 : 设计一个“伪任务”（Pretext Task）。这个任务就是“用词推理词”。模型并不真的需要成为一个完美的“预言家”。

3. 学习 :

• 模型有一个巨大的 “词向量查找表” （就像你的美食地图），初始值是随机的。

• 它拿到一句话，比如 the cat sat on the mat 。

• 它取出中心词 sat ，任务是预测周围的词 the, cat, on, mat 。

• 它用 sat 的当前向量去做预测，发现预测错了。

• 为了下次能做得更好，它会微调 sat , the , cat , on , mat 这些词的向量。

4. 副产品 : 当模型在数十亿的句子上重复这个“预测-犯错-微调”的过程亿万次后，那个初始随机的“词向量查找表”就被打磨得非常精确了。出现在相似上下文语境中的词（如 cat 和 dog ），它们的向量因为经历了相似的调整，最终在向量空间中的位置就自然而然地聚集到了一起。

总结一下：

论文里讲的“用词推理词”不是最终目的。它是一个 巧妙设计的训练任务 ，目的是 强迫 模型在完成这个任务的过程中，不得不去学习和调整每个词的向量。我们真正想要的，不是那个预测模型本身，而是它在训练结束后留下的那个包含了所有词的高质量向量的 “查找表” 。这个查找表，就是 Word2Vec 的精髓。

好了，再回到论文。

---

第五页：模型架构图与结果初步介绍

这一页非常关键，它用一张图清晰地展示了第四页提出的两个模型（CBOW 和 Skip-gram）的结构差异，并开始引入如何评测这些模型效果的话题。

图1：新模型架构 (Figure 1: New model architectures)

![image]()

这张图是理解CBOW和Skip-gram最直观的方式。

• 左侧：CBOW (Continuous Bag-of-Words)

• 输入 (INPUT) : w(t-2) , w(t-1) , w(t+1) , w(t+2) 。这些是中心词 w(t) 周围的上下文单词。

• 投影 (PROJECTION) : 输入的这些单词向量在投影层被简单地 求和/平均（SUM） 。这就是“词袋”思想的体现，把所有上下文信息揉合成一个向量。

• 输出 (OUTPUT) : 模型利用这个聚合后的上下文向量，来预测中心词 w(t) 。

• 信息流 : 多对一 (Many-to-One) 。从多个上下文词，预测一个中心词。

• 右侧：Skip-gram

• 输入 (INPUT) : 中心词 w(t) 。

• 投影 (PROJECTION) : 只有一个输入词，所以投影层只是简单地传递它的向量。
---

[上一页](076-文字如何变成向量？向量如何代表语义？向量怎么算出来的？Word2Vec这篇论文都说清了（1-5）.md) | [目录](../index.md) | [下一页](078-文字如何变成向量？向量如何代表语义？向量怎么算出来的？Word2Vec这篇论文都说清了（3-5）.md)
